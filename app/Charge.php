<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Charge extends Model{
    // Si generas la tabla por el script de sql y no funciona descomenta la linea de abajo para que enlace la tabla
    // o corre la migracion creada
    //protected $table = 'charges';
}
